version: "3.8"
services:
  db:
    image: postgres:latest
    ports:
      - 5432:5432
    env_file: 
      - ./.env
    volumes:
      - ./routes.sql:/docker-entrypoint-initdb.d/routes.sql
      - "./routes.csv:/var/lib/postgresql/data/pgdata"
